---
title: Spring DB
tags: ['Spring', 'Java']
---

## 데이터 접근 기술 - 시작

### 데이터 접근 기술 분류

실무에서 쓰는 데이터 접근 기술은 크게 두 갈래로 나뉜다.

-   **SQL Mapper**: `JdbcTemplate`, `MyBatis`
    -   개발자가 SQL을 직접 작성하면, 그 결과를 객체로 매핑해준다. JDBC 직접 사용 시의 반복을 제거해준다.
-   **ORM**: `JPA / Hibernate`, `스프링 데이터 JPA`, `Querydsl`
    -   기본적인 SQL은 JPA가 대신 작성·처리한다. 객체를 자바 컬렉션에 저장·조회하듯 다루면 ORM이 DB에 반영한다.
    -   JPA는 자바 ORM 표준(인터페이스), Hibernate는 가장 많이 쓰는 구현체다.
    -   스프링 데이터 JPA, Querydsl은 JPA를 더 편리하게 쓰게 돕는 프로젝트로, 실무에서 JPA를 쓴다면 사실상 필수다.

:::tip JDBC와 ORM(JPA)의 관계
둘 다 자바 표준 인터페이스지만, 경쟁 관계가 아니라 **추상화 수준이 다른 위아래 레이어**다.

-   **JDBC**: DB와 통신하는 저수준 표준. SQL을 직접 작성하고 결과를 객체에 수동 매핑한다. 구현체는 각 DB **드라이버**(H2, MySQL 등).
-   **JPA**: ORM을 위한 고수준 표준. 기본 SQL을 자동 생성해주고, 객체를 컬렉션 다루듯 저장·조회한다. 구현체는 **Hibernate** 등.

핵심은 **JPA도 내부적으로는 JDBC를 사용한다**는 점이다. JPA가 SQL을 만들어 JDBC 드라이버를 통해 DB로 보낸다. `JdbcTemplate`·MyBatis는 JDBC 바로 위에 얹은 편의 도구(SQL은 직접 작성)이고, JPA는 한 층 더 위에서 SQL 작성 자체를 대신한다.

```text
내 코드 → JPA/Hibernate → JDBC → DB 드라이버 → DB
```

:::

이번 장의 목표는 세세한 기능이 아니라 **각 기술이 왜 필요한지, 장단점이 무엇인지** 파악하는 것이다. 메모리 기반으로 완성된 프로젝트에 데이터 접근 기술을 하나씩 점진적으로 도입하며 비교한다.

### 인터페이스 기반 구조

구현 기술을 손쉽게 교체하기 위해 리포지토리를 인터페이스로 추상화한다.

```java
public interface ItemRepository {
    Item save(Item item);
    void update(Long itemId, ItemUpdateDto updateParam);
    Optional<Item> findById(Long id);
    List<Item> findAll(ItemSearchCond cond);
}
```

-   `findById`는 값이 없을 수 있으므로 `Optional`을 반환한다.
-   검색 조건 객체는 `Cond`(condition) 접미사로 통일했다(`ItemSearchCond`). `like` 검색을 위해 상품명 일부만 포함해도 검색되어야 한다.
-   단순 데이터 전달용 객체는 `Dto` 접미사를 붙인다(`ItemUpdateDto`).

:::tip 네이밍 규칙
DTO·Cond 같은 접미사 규칙은 정해진 표준이 없다. **프로젝트 안에서 일관성 있게** 정하면 된다. `ItemSearchCond`처럼 이미 용도가 드러나면 굳이 `Dto`를 중복으로 붙이지 않는다.
:::

서비스도 같은 이유(예제에서 구현체 교체)로 인터페이스를 도입했지만, **실무에서 서비스는 구현체를 바꿀 일이 많지 않아 인터페이스를 잘 도입하지 않는다.**

### 설정과 빈 등록 전략

서비스·리포지토리는 구현체를 편리하게 교체하기 위해 `@Configuration`에서 **수동으로 빈 등록**하고, 컨트롤러만 컴포넌트 스캔을 사용한다.

```java
@Configuration
public class MemoryConfig {
    @Bean
    public ItemService itemService() {
        return new ItemServiceV1(itemRepository());
    }
    @Bean
    public ItemRepository itemRepository() {
        return new MemoryItemRepository();
    }
}
```

```java
@Import(MemoryConfig.class)
@SpringBootApplication(scanBasePackages = "hello.itemservice.web")
public class ItemServiceApplication { ... }
```

-   `@Import`로 설정 파일을 지정하고, `scanBasePackages`로 컴포넌트 스캔 범위를 `web` 하위(컨트롤러)로 제한한다.

### 초기 데이터 등록과 @EventListener

확인용 초기 데이터는 `@EventListener(ApplicationReadyEvent.class)`로 등록한다.

```java
@Slf4j
@RequiredArgsConstructor
public class TestDataInit {
    private final ItemRepository itemRepository;

    @EventListener(ApplicationReadyEvent.class)
    public void initData() {
        itemRepository.save(new Item("itemA", 10000, 10));
        itemRepository.save(new Item("itemB", 20000, 20));
    }
}
```

:::warning @PostConstruct 대신 ApplicationReadyEvent
`@PostConstruct`는 AOP가 아직 다 적용되지 않은 시점에 호출될 수 있어, `@Transactional` 같은 AOP가 적용되지 않은 상태로 실행되는 문제가 생길 수 있다.
`@EventListener(ApplicationReadyEvent.class)`는 AOP를 포함해 스프링 컨테이너가 완전히 초기화된 뒤 호출되므로 안전하다.
:::

:::tip AOP란?

AOP(Aspect-Oriented Programming, 관점 지향 프로그래밍)는 여러 곳에 흩어질 공통 기능(횡단 관심사)을 따로 모아, 핵심 로직을 건드리지 않고 자동으로 끼워 넣는 방식을 말한다.

트랜잭션, 로깅, 보안 검사, 성능 측정 같은 기능들은 여러 메서드·여러 클래스에 공통으로 필요한데, 정작 비즈니스 로직과는 성격이 다른 별개의 관심사이다. 이런 걸 **횡단 관심사(cross-cutting concern)**라고 부른다. 여러 코드를 "가로질러(cross)" 반복적으로 등장하기 때문이다.

이걸 그냥 두면 어떻게 되는지는 앞서 본 "프록시 도입 전" 트랜잭션 코드가 잘 보여준다.

메서드마다 getTransaction → try → commit → catch → rollback이 똑같이 반복되고, 비즈니스 로직과 뒤엉킨다. AOP는 이 공통 기능을 한 군데 모아 정의해두고, 필요한 지점에 자동으로 끼워 넣자는 발상이다.

:::

### 프로필로 환경 분리

환경(로컬/운영/테스트)별로 다른 설정·빈을 적용할 때 프로필을 쓴다. 스프링은 `application.properties`의 `spring.profiles.active`를 읽는다.

-   `src/main/resources` → `spring.profiles.active=local` (앱 직접 실행 시)
-   `src/test/resources` → `spring.profiles.active=test` (테스트 실행 시)
-   `@Profile("local")`이 붙은 빈은 해당 프로필일 때만 등록된다.

```java
@Bean
@Profile("local")
public TestDataInit testDataInit(ItemRepository itemRepository) {
    return new TestDataInit(itemRepository);
}
```

:::tip 프로필로 테스트 오염 방지
초기 데이터(`TestDataInit`)는 `local`에서만 등록된다. 테스트는 `test` 프로필로 실행되므로 초기 데이터가 들어가지 않는다.
이게 없으면 "1건 저장 후 카운트 검증"이 초기 데이터 2건 때문에 3이 되어 테스트가 깨진다.
:::

### 테스트 작성 원칙

```java
@SpringBootTest
class ItemRepositoryTest {
    @Autowired
    ItemRepository itemRepository;

    @AfterEach
    void afterEach() {
        if (itemRepository instanceof MemoryItemRepository) {
            ((MemoryItemRepository) itemRepository).clearStore();
        }
    }
}
```

-   테스트는 서로 영향을 주면 안 되므로 `@AfterEach`로 데이터를 초기화한다. 메모리 저장소는 인터페이스에 `clearStore()`가 없어 다운캐스팅으로 처리하지만, **실제 DB는 테스트 후 트랜잭션 롤백으로 초기화**한다.
-   구현체(`MemoryItemRepository`)가 아니라 **인터페이스(`ItemRepository`)를 대상으로 테스트**한다. 그러면 구현체가 바뀌어도 같은 테스트로 검증할 수 있다.
-   `findAll` 검색은 `null`뿐 아니라 빈 문자열(`""`) 조건도 잘 동작하는지 검증한다.

### DB 테이블 생성과 식별자 전략

```sql
create table item
(
    id bigint generated by default as identity,
    item_name varchar(10),
    price integer,
    quantity integer,
    primary key (id)
);
```

-   `generated by default as identity`: 기본 키 생성을 DB에 위임하는 identity 전략. MySQL의 Auto Increment와 같다. PK는 비워두고 저장하면 DB가 증가값을 채운다.

기본 키는 (1) null 불가, (2) 유일, (3) 불변 세 조건을 모두 만족해야 한다. 키 선택 전략은 두 가지다.

-   **자연 키(natural key)**: 비즈니스 의미가 있는 키 (주민번호, 이메일, 전화번호)
-   **대리 키(surrogate key)**: 비즈니스와 무관한 임의 키 (identity, auto_increment, 시퀀스)

:::danger 대리 키를 권장
자연 키는 그럴듯해 보여도 비즈니스 규칙 변화에 취약하다. 주민번호를 PK로 잡으면, 정책 변경으로 저장이 금지될 경우 이를 FK로 참조하던 수많은 테이블과 애플리케이션 로직을 전부 고쳐야 한다.
대리 키는 비즈니스와 무관해 요구사항이 변해도 PK가 바뀔 일이 드물다. **대리 키를 PK로 쓰되, 자연 키 후보(이메일·주민번호 등)는 유니크 인덱스로 관리**하는 것을 권장한다. JPA도 모든 엔티티에 일관된 대리 키 사용을 권장한다.
:::

## 스프링 JdbcTemplate

### JdbcTemplate 개요

SQL을 직접 작성하는 경우 가장 간단하고 실용적인 선택지다. 템플릿 콜백 패턴으로 JDBC의 반복 작업(커넥션 획득/종료, statement 준비·실행, ResultSet 루프, 트랜잭션 커넥션 동기화, 예외 변환)을 대신 처리해준다. 개발자는 **SQL 작성, 파라미터 정의, 결과 매핑**만 하면 된다.

-   장점: `spring-jdbc`에 포함되어 별도 설정 없이 바로 사용. 반복 코드 제거.
-   단점: **동적 쿼리 작성이 어렵다.** SQL을 자바 문자열로 작성해 줄이 넘어갈 때마다 문자열 더하기를 해야 한다.

설정은 `spring-boot-starter-jdbc`와 DB 드라이버(`com.h2database:h2`)만 추가하면 된다. `application.properties`에 `datasource.url`만 설정하면 스프링 부트가 커넥션 풀·`DataSource`·트랜잭션 매니저를 자동 등록한다.

```groovy
implementation 'org.springframework.boot:spring-boot-starter-jdbc'
runtimeOnly 'com.h2database:h2'
```

### V1 — 기본 JdbcTemplate (순서 기반 바인딩)

`DataSource`를 주입받아 생성자 내부에서 `JdbcTemplate`을 생성하는 것이 관례다.

```java
public JdbcTemplateItemRepositoryV1(DataSource dataSource) {
    this.template = new JdbcTemplate(dataSource);
}
```

주요 메서드는 다음과 같다.

-   `template.update(sql, 파라미터...)`: INSERT/UPDATE/DELETE. 반환값은 영향받은 로우 수(int). `?`에 순서대로 바인딩.
-   `template.queryForObject(sql, rowMapper, args)`: 단건 조회. 결과 없으면 `EmptyResultDataAccessException`, 둘 이상이면 `IncorrectResultSizeDataAccessException`.
-   `template.query(sql, rowMapper, args)`: 목록 조회. 결과 없으면 빈 컬렉션.

`findById`는 인터페이스가 `Optional` 반환을 요구하므로, 예외를 잡아 `Optional.empty()`로 변환한다.

```java
public Optional<Item> findById(Long id) {
    String sql = "select id, item_name, price, quantity from item where id = ?";
    try {
        Item item = template.queryForObject(sql, itemRowMapper(), id);
        return Optional.of(item);
    } catch (EmptyResultDataAccessException e) {
        return Optional.empty();
    }
}
```

identity(auto increment) PK는 INSERT가 완료돼야 값이 생기므로, `KeyHolder`로 생성된 키를 조회한다. KeyHolder는 DB가 자동 생성한 PK 값을 받아오기 위한 객체이다.

```java
KeyHolder keyHolder = new GeneratedKeyHolder();
template.update(connection -> {
    PreparedStatement ps = connection.prepareStatement(sql, new String[]{"id"});
    ps.setString(1, item.getItemName());
    // ...
    return ps;
}, keyHolder);
long key = keyHolder.getKey().longValue();
```

RowMapper는 `ResultSet`을 객체로 변환한다. V1은 수동으로 작성한다.

```java
private RowMapper<Item> itemRowMapper() {
    return (rs, rowNum) -> {
        Item item = new Item();
        item.setId(rs.getLong("id"));
        item.setItemName(rs.getString("item_name"));
        // ...
        return item;
    };
}
```

:::tip RowMapper 동작
JdbcTemplate이 `while(ResultSet 끝까지)` 루프를 돌려주고, 개발자는 한 행을 객체로 바꾸는 RowMapper 내부만 채운다.
:::

### V2 — NamedParameterJdbcTemplate (이름 기반 바인딩, 권장)

V1 대비 핵심 변화는 **`?` 순서 바인딩 → `:이름` 이름 바인딩**이다.

:::danger 순서 바인딩의 위험
순서 바인딩은 SQL 컬럼 순서와 파라미터 전달 순서가 어긋나면 엉뚱한 값이 들어간다. 예를 들어 SQL에서 `price`와 `quantity` 순서만 바꿨는데 파라미터는 그대로면, 두 값이 뒤바뀌어 저장된다.
파라미터가 10~20개를 넘거나 나중에 필드를 추가·수정할 때 충분히 터질 수 있다. **DB에 데이터가 잘못 들어가는 버그는 코드뿐 아니라 데이터까지 복구해야 해 가장 비싸다.** 그래서 이름 기반 바인딩(`NamedParameterJdbcTemplate`)이 권장된다.
:::

```java
public JdbcTemplateItemRepositoryV2(DataSource dataSource) {
    this.template = new NamedParameterJdbcTemplate(dataSource);
}

// SQL이 ? 대신 :이름
String sql = "insert into item (item_name, price, quantity) " +
             "values (:itemName, :price, :quantity)";
```

이름 파라미터에 값을 넘기는 방식은 3가지다.

-   **`Map`**: 가장 단순. `Map.of("id", id)`.
-   **`MapSqlParameterSource`**: Map과 유사하나 SQL 타입 지정 등 특화 기능 제공. 메서드 체인 사용.
-   **`BeanPropertySqlParameterSource`**: 자바빈 프로퍼티 규약으로 객체에서 파라미터를 자동 생성(`getItemName()` → `itemName`).

```java
// BeanProperty — 객체 프로퍼티를 자동 매핑
SqlParameterSource param = new BeanPropertySqlParameterSource(item);

// MapSqlParameterSource — 체인으로 직접 지정
SqlParameterSource param = new MapSqlParameterSource()
        .addValue("itemName", updateParam.getItemName())
        .addValue("price", updateParam.getPrice())
        .addValue("id", itemId); // DTO에 없는 값을 별도로 추가
```

:::warning BeanPropertySqlParameterSource를 항상 쓸 수는 없다

`update()`는 SQL에 `:id`가 필요한데 `ItemUpdateDto`에는 `itemId`만 있고 `id`가 없다. 객체에 대응 프로퍼티가 없으면 자동 매핑이 불가하므로, 이런 경우 `MapSqlParameterSource`로 직접 값을 넣는다.

-   SQL 쿼리(파라미터명 :id 포함)가 먼저 존재한다
-   런타임 객체로 바인딩할 때 객체 속성명 ↔ `:파라미터명`을 비교한다
-   조회 쿼리 시 AS를 사용하여 별칭을 지정해두면 최종 비교 대상은 별칭이 된다. 컬럼명은 비교 대상이 아니다.

:::

RowMapper도 V1의 수동 작성에서 `BeanPropertyRowMapper`로 바뀐다. 조회 결과 컬럼명 기반으로 setter를 자동 호출한다(리플렉션 사용).

```java
private RowMapper<Item> itemRowMapper() {
    return BeanPropertyRowMapper.newInstance(Item.class);
}
```

:::tip snake_case ↔ camelCase 자동 변환과 별칭
`BeanPropertyRowMapper`는 DB의 `snake_case`를 객체의 `camelCase`로 자동 변환한다(`item_name` → `setItemName()`). 따라서 snake_case는 그냥 두면 된다.
다만 컬럼명과 객체 이름이 완전히 다르면(`member_name` ↔ `username`) 조회 SQL에서 별칭을 쓴다: `select member_name as username`. 실무에서 자주 쓰는 해결법이다.
:::

### V3 — SimpleJdbcInsert (INSERT SQL 자동 생성)

V2 대비 **INSERT만 `SimpleJdbcInsert`로 대체**된다(update/findById/findAll은 V2와 동일). INSERT SQL을 직접 작성하지 않아도 된다.

```java
public JdbcTemplateItemRepositoryV3(DataSource dataSource) {
    this.template = new NamedParameterJdbcTemplate(dataSource);
    this.jdbcInsert = new SimpleJdbcInsert(dataSource)
            .withTableName("item")
            .usingGeneratedKeyColumns("id");
}

public Item save(Item item) {
    SqlParameterSource param = new BeanPropertySqlParameterSource(item);
    Number key = jdbcInsert.executeAndReturnKey(param); // 생성 키도 바로 반환
    item.setId(key.longValue());
    return item;
}
```

-   `withTableName`: 저장할 테이블명. `usingGeneratedKeyColumns`: PK 컬럼명.
-   `usingColumns`: INSERT에 쓸 컬럼 지정(생략 가능). `SimpleJdbcInsert`는 생성 시점에 테이블 메타데이터를 조회해 컬럼을 파악하므로 보통 생략한다.
-   `executeAndReturnKey`로 INSERT 실행과 생성 키 조회를 한 번에 처리해, V1의 `KeyHolder` 보일러플레이트가 사라진다.

### 동적 쿼리 문제

`findAll`은 검색 조건(itemName, maxPrice)의 유무에 따라 4가지 SQL을 만들어야 한다. 조건 조합에 따라 `where`/`and`를 직접 계산하고 파라미터도 맞춰 생성해야 한다.

```java
String sql = "select id, item_name, price, quantity from item";
if (StringUtils.hasText(itemName) || maxPrice != null) {
    sql += " where";
}
boolean andFlag = false;
if (StringUtils.hasText(itemName)) {
    sql += " item_name like concat('%',:itemName,'%')";
    andFlag = true;
}
if (maxPrice != null) {
    if (andFlag) sql += " and";
    sql += " price <= :maxPrice";
}
```

조건이 늘수록 분기가 급격히 복잡해진다. 이 동적 쿼리 문제를 깔끔하게 푸는 것이 다음에 배울 **MyBatis**다.

### 기능 정리

-   `JdbcTemplate`: 순서 기반 바인딩
-   `NamedParameterJdbcTemplate`: 이름 기반 바인딩 (권장)
-   `SimpleJdbcInsert`: INSERT 편의 기능
-   `SimpleJdbcCall`: 스토어드 프로시저 호출

조회는 단건이 `queryForObject()`(숫자·문자는 `Integer.class` 등 타입 지정, 객체는 RowMapper), 목록이 `query()`다. 변경은 모두 `update()`, 임의 DDL은 `execute()`를 쓴다. RowMapper를 필드로 분리하면 여러 곳에서 재사용할 수 있다.

:::tip JdbcTemplate의 위치
JPA 같은 ORM을 쓰면서도 SQL을 직접 써야 할 때 JdbcTemplate을 함께 쓰면 좋다. 다만 동적 쿼리에 약하고 SQL이 자바 문자열이라는 한계가 있어, 그 부분은 MyBatis가 보완한다.
:::

## 데이터 접근 기술 - 테스트

### 테스트 환경 설정 (DB 연동)

테스트 코드는 `src/test` 하위에 있으므로 실행 시 `src/test/resources/application.properties`가 우선순위를 가진다. 따라서 테스트에서도 DB에 접속하려면 해당 파일에 datasource 설정을 넣어야 한다.

```properties
# src/test/resources/application.properties
spring.profiles.active=test
spring.datasource.url=jdbc:h2:tcp://localhost/~/test
spring.datasource.username=sa
logging.level.org.springframework.jdbc=debug
```

`@SpringBootTest`는 `@SpringBootApplication`을 찾아 설정으로 사용한다. 따라서 메인 설정(Config)이 바뀌면 테스트도 같은 구현체를 사용하게 된다.

:::tip TestDataInit은 테스트에서 동작하지 않음
`TestDataInit`은 프로필이 `local`일 때만 동작한다. 테스트는 `spring.profiles.active=test`이므로 초기화 데이터가 추가되지 않는다.
:::

### 테스트 DB 분리

로컬 애플리케이션 서버와 테스트가 같은 DB를 쓰면 기존에 저장된 데이터가 테스트에 영향을 준다. 핵심 해결 원칙은 **테스트 전용 DB를 분리**하는 것이다.

```properties
# local 전용
jdbc:h2:tcp://localhost/~/test

# test 케이스 전용
jdbc:h2:tcp://localhost/~/testcase
```

:::warning main 설정은 건드리지 않는다
`main`의 application.properties는 그대로 두고, **`test`의 application.properties만** testcase로 변경해야 한다. testcase DB에도 동일한 item 테이블을 미리 생성해두어야 한다.
:::

### 테스트 격리의 두 가지 원칙

DB를 분리해도 테스트를 반복 실행하면 이전 테스트가 저장한 데이터가 남아 다음 테스트를 오염시킨다.

:::tip 테스트 핵심 원칙

-   테스트는 다른 테스트와 **격리**해야 한다
-   테스트는 **반복 실행** 가능해야 한다

:::

`DELETE SQL`로 직접 지우는 방법은 근본 해결책이 아니다. 테스트 도중 예외가 발생하거나 애플리케이션이 종료되면 삭제 로직이 호출되지 않아 데이터가 남는다.

### 트랜잭션 롤백 전략

테스트가 끝난 뒤 트랜잭션을 강제 롤백하면 데이터가 깔끔하게 제거된다. 중간에 테스트가 실패해도 커밋하지 않았으므로 DB에 반영되지 않아 안전하다.

```text
1. 트랜잭션 시작
2. 테스트 A 실행
3. 트랜잭션 롤백
```

`@BeforeEach`/`@AfterEach`로 트랜잭션을 직접 제어할 수 있다.

```java
@SpringBootTest
class ItemRepositoryTest {
    @Autowired ItemRepository itemRepository;
    @Autowired PlatformTransactionManager transactionManager;
    TransactionStatus status;

    @BeforeEach
    void beforeEach() {
        status = transactionManager.getTransaction(new DefaultTransactionDefinition());
    }

    @AfterEach
    void afterEach() {
        transactionManager.rollback(status);
    }
}
```

:::tip 트랜잭션 매니저 자동 등록
스프링 부트가 적절한 `PlatformTransactionManager`를 빈으로 자동 등록해주므로 주입만 받으면 된다.
:::

### @Transactional (실무 표준 방식)

위의 트랜잭션 제어 코드를 `@Transactional` 애노테이션 하나로 대체할 수 있다. 이것이 실무에서 사용하는 방식이다.

```java
import org.springframework.transaction.annotation.Transactional;

@Transactional
@SpringBootTest
class ItemRepositoryTest {}
```

:::tip 테스트에서의 특별 동작
`@Transactional`은 일반 로직에서는 성공 시 커밋하지만, **테스트에 붙으면** 테스트를 트랜잭션 안에서 실행한 뒤 **끝나면 자동 롤백**한다.
:::

동작 순서:

1. 테스트 시작 시 트랜잭션 시작
2. 모든 로직이 같은 트랜잭션 안에서 수행 (리포지토리의 JdbcTemplate도 트랜잭션 전파로 동일 트랜잭션 사용)
3. INSERT로 데이터 저장
4. SELECT로 조회·검증 (같은 트랜잭션이므로 조회 가능, 다른 트랜잭션에서는 안 보임)
5. 테스트 종료 시 강제 롤백
6. 저장했던 데이터 제거

서비스·리포지토리에 있는 `@Transactional`도 테스트에서 시작한 트랜잭션에 참여한다. 즉 같은 트랜잭션 = 같은 커넥션을 사용한다.

클래스 레벨에 `@Transactional` 애노테이션을 추가해도 메서드 단위로 롤백이 수행된다.

### 강제 커밋 - @Commit

DB에 데이터가 잘 들어갔는지 직접 확인하고 싶을 때는 `@Commit`(또는 `@Rollback(value = false)`)을 붙이면 롤백 대신 커밋된다.

```java
import org.springframework.test.annotation.Commit;

@Commit
@Transactional
@SpringBootTest
class ItemRepositoryTest {}
```

### 임베디드 모드 DB

H2는 JVM 메모리 안에서 동작하는 임베디드 모드를 지원한다. 애플리케이션과 함께 실행되고, 종료되면 DB와 데이터도 함께 사라진다. 테스트 검증용으로 별도 DB를 설치·운영할 필요가 없다.

직접 설정하는 경우 핵심 URL은 다음과 같다.

```java
@Bean
@Profile("test")
public DataSource dataSource() {
    DriverManagerDataSource dataSource = new DriverManagerDataSource();
    dataSource.setDriverClassName("org.h2.Driver");
    dataSource.setUrl("jdbc:h2:mem:db;DB_CLOSE_DELAY=-1");
    dataSource.setUsername("sa");
    dataSource.setPassword("");
    return dataSource;
}
```

:::tip URL 핵심

-   `jdbc:h2:mem:db` → 메모리(임베디드) 모드로 동작
-   `DB_CLOSE_DELAY=-1` → 커넥션이 모두 끊겨도 DB가 종료되지 않도록 방지

:::

### 임베디드 DB 테이블 생성 (schema.sql)

메모리 DB는 매번 새로 만들어지므로 테이블이 없어 `Table "ITEM" not found` 오류가 난다. 스프링 부트의 SQL 스크립트 초기화 기능으로 해결한다.

:::warning 파일 위치와 이름 주의
`src/test/resources/schema.sql` 위치와 파일명이 정확해야 동작한다.
:::

```sql
drop table if exists item CASCADE;
create table item
(
    id bigint generated by default as identity,
    item_name varchar(10),
    price integer,
    quantity integer,
    primary key (id)
);
```

### 스프링 부트 임베디드 모드 (설정 생략)

스프링 부트는 DB 설정이 전혀 없으면 자동으로 임베디드 DB를 사용한다. 직접 만든 dataSource 빈과 test의 datasource 설정을 모두 주석 처리하면 된다.

```properties
# src/test/resources/application.properties
spring.profiles.active=test
#spring.datasource.url=jdbc:h2:tcp://localhost/~/testcase
#spring.datasource.username=sa
logging.level.org.springframework.jdbc=debug
```

:::tip 임베디드 DB 이름 고정
기본적으로 `jdbc:h2:mem:` 뒤에 충돌 방지용 임의 이름이 붙는다. 고정하려면 아래 설정을 추가한다.
:::

```properties
spring.datasource.generate-unique-name=false
```

:::warning 스프링 부트 3.x 트랜잭션 로깅
트랜잭션 시작/롤백 로그가 안 보이면 로깅 레벨이 INFO에서 TRACE로 변경된 것이다. 다음 설정을 추가한다.
:::

```properties
logging.level.org.springframework.test.context.transaction=trace
```

## 데이터 접근 기술 - MyBatis

### MyBatis란

MyBatis는 JdbcTemplate보다 더 많은 기능을 제공하는 **SQL Mapper**다. JdbcTemplate의 기능 대부분을 포함하면서, 두 가지가 핵심 강점이다.

-   SQL을 XML에 작성 → 여러 줄 SQL에서 자바 문자열 더하기(`+`)의 불편함이 사라짐
-   **동적 쿼리**를 `<if>`, `<where>` 같은 태그로 매우 편리하게 작성

:::tip 선택 기준
동적 쿼리·복잡한 쿼리가 많으면 MyBatis, 단순한 쿼리 위주면 JdbcTemplate. 둘을 함께 써도 되지만, MyBatis를 택했다면 그것으로 충분하다.
:::

### 설정

`mybatis-spring-boot-starter` 의존성을 추가한다. 스프링 부트가 버전 관리하는 공식 라이브러리가 아니라서 **버전을 직접 명시**해야 한다.

```groovy
// 스프링 부트 3.0 이상
implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:3.0.3'
// 스프링 부트 4.0 이상
implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:4.0.1'
```

`application.properties` 설정의 핵심은 두 가지다.

```properties
mybatis.type-aliases-package=hello.itemservice.domain
mybatis.configuration.map-underscore-to-camel-case=true
logging.level.hello.itemservice.repository.mybatis=trace
```

-   `type-aliases-package`: XML `resultType` 등에서 패키지명을 생략할 수 있게 함 (하위 패키지 자동 인식)
-   `map-underscore-to-camel-case`: `item_name`(snake) → `itemName`(camel) 자동 변환. `BeanPropertyRowMapper`와 같은 역할

:::warning main / test 양쪽 모두 수정
`main`과 `test` 각 위치의 `application.properties`를 **둘 다** 수정해야 한다. 설정 반영이 안 되면 거의 이 문제다.
:::

:::tip 이름 매핑 정리
snake_case ↔ camelCase는 옵션으로 자동 해결되니 그냥 두면 되고, 컬럼명과 객체명이 **완전히 다를 때만** 조회 SQL에서 별칭(`select item_name as name`)을 쓴다. 앞서 JdbcTemplate에서 본 별칭 원리가 그대로 적용된다.
:::

### 매퍼 인터페이스와 XML

매퍼 인터페이스에 `@Mapper`를 붙이고, 같은 이름의 XML에 SQL을 작성한다. 인터페이스 메서드를 호출하면 XML의 해당 SQL이 실행된다.

```java
@Mapper
public interface ItemMapper {
    void save(Item item);
    void update(@Param("id") Long id, @Param("updateParam") ItemUpdateDto updateParam);
    Optional<Item> findById(Long id);
    List<Item> findAll(ItemSearchCond itemSearch);
}
```

```xml
<mapper namespace="hello.itemservice.repository.mybatis.ItemMapper">

    <insert id="save"> ... </insert>        <!-- save() 메서드와 매칭 -->
    <update id="update"> ... </update>      <!-- update() 메서드와 매칭 -->
    <select id="findById"> ... </select>    <!-- findById() 메서드와 매칭 -->
    <select id="findAll"> ... </select>     <!-- findAll() 메서드와 매칭 -->

</mapper>
```

XML은 자바 코드가 아니므로 `src/main/resources` 하위에 두되 **패키지 경로를 인터페이스와 동일하게** 맞춘다. `namespace`에는 매퍼 인터페이스의 전체 경로를 지정한다.

:::tip XML 위치 자유롭게 두기
경로 맞추는 게 번거로우면 `mybatis.mapper-locations=classpath:mapper/**/*.xml` 설정으로 위치와 파일명을 자유롭게 둘 수 있다. (이때도 test의 properties까지 함께 수정)
:::

### CRUD XML 작성 핵심

파라미터는 `#{}` 문법을 쓴다. 이건 `PreparedStatement`의 `?`로 치환된다고 보면 된다. 매퍼에서 넘긴 객체의 프로퍼티명을 적는다.

```xml
<insert id="save" useGeneratedKeys="true" keyProperty="id">
    insert into item (item_name, price, quantity)
    values (#{itemName}, #{price}, #{quantity})
</insert>
```

-   위 코드에서는 `save(Item item)` 타입의 메서드에 대해 Item 객체의 속성들을 속성명 비교를 통해 쿼리 파라미터에 값을 넣어준다.
    -   `update(Item item, int id)`와 같이 파라미터가 둘 이상일 때는 `@Param`을 통해 명시해야 하고, xml 내에서는 `#{item.itemName}`, `#{id}`로 접근해야 한다.
-   `id`: 매퍼 인터페이스의 메서드명과 일치시킴
-   `useGeneratedKeys` + `keyProperty="id"`: DB가 키를 생성하는 IDENTITY 전략일 때 사용. INSERT 후 생성된 키가 객체의 `id`에 채워짐 (JdbcTemplate의 KeyHolder 역할)

파라미터가 2개 이상이면 `@Param`으로 이름을 지정해 구분해야 한다(1개면 생략 가능). XML에서는 `#{updateParam.itemName}`처럼 점 표기로 접근한다.

```xml
<select id="findById" resultType="Item">
    select id, item_name, price, quantity from item where id = #{id}
</select>
```

`resultType`으로 반환 타입을 지정하면 결과를 객체로 자동 매핑한다. 반환이 하나면 `Item`/`Optional<Item>`, 여럿이면 `List`를 쓴다.

함수 반환 타입이 리스트이면 MyBatis가 자동으로 반환 결과를 리스트 내에 담아준다는 의미이다.

### 동적 쿼리 (MyBatis의 핵심)

JdbcTemplate은 자바 코드로 if문을 붙여가며 SQL을 조립해야 하지만, MyBatis는 태그로 처리한다.

```xml
<select id="findAll" resultType="Item">
    select id, item_name, price, quantity from item
    <where>
        <if test="itemName != null and itemName != ''">
            and item_name like concat('%',#{itemName},'%')
        </if>
        <if test="maxPrice != null">
            and price &lt;= #{maxPrice}
        </if>
    </where>
</select>
```

-   `<if>`: 조건이 참이면 구문 추가 (내부 문법은 OGNL)
-   `<where>`: 내부에 조건이 하나라도 있으면 `where`를 붙이고, 맨 앞의 `and`를 자동으로 제거. 조건이 모두 실패하면 `where` 자체를 안 만듦

OGNL은 Object-Graph Navigation Language의 약자이다. 객체의 속성에 접근하고 간단한 조건식을 평가하는 표현식 언어(expression language) 이다. MyBatis의 `<if test="...">` 안에 들어가는 그 문법이 OGNL이다. (`itemName != null and itemName != '')`)

:::warning XML 특수문자
XML에서는 `<`, `>`, `&`를 직접 쓸 수 없어 `&lt;`, `&gt;`, `&amp;`로 이스케이프한다. 또는 `<![CDATA[ ... ]]>` 안에 넣으면 특수문자를 그대로 쓸 수 있지만, **CDATA 안에서는 `<if>` 같은 동적 태그가 동작하지 않는다.**

비교연산을 수행할 때 이스케이프 문자를 직접 사용하거나 CDATA 구문을 활용해야 한다.
:::

이 외 동적 SQL 태그로 `choose`(when/otherwise, 자바 switch 유사), `trim`(`<where>`/`<set>`을 직접 커스텀), `foreach`(컬렉션 반복 → `where id in (1,2,3)` 생성)가 있다.

```xml
<foreach item="item" collection="list" open="ID in (" separator="," close=")">
    #{item}
</foreach>
```

### 구현체 없이 동작하는 원리

`ItemMapper`는 인터페이스만 있고 구현체를 작성하지 않는데도 동작한다. MyBatis 스프링 연동 모듈이 자동 처리하기 때문이다.

1. 로딩 시점에 `@Mapper`가 붙은 인터페이스를 조사
2. **동적 프록시** 기술로 구현체를 생성 (실제 로그를 찍으면 `com.sun.proxy.$Proxy...` 로 JDK 동적 프록시 확인 가능)
3. 생성된 구현체를 스프링 빈으로 등록

이 매퍼 구현체는 **예외 변환도 처리**한다. MyBatis 예외를 스프링 예외 추상화(`DataAccessException`)로 변환해준다(JdbcTemplate과 동일한 이점). 커넥션·트랜잭션 동기화도 함께 연동된다.

### 기타 기능

애노테이션으로 SQL을 작성할 수도 있다. 단 **동적 SQL이 안 되므로 간단한 경우에만** 쓴다.

```java
@Select("select id, item_name, price, quantity from item where id=#{id}")
Optional<Item> findById(Long id);
```

:::danger ${} 문자열 대체 — SQL 인젝션 위험
`#{}`는 `PreparedStatement` 바인딩(안전)이지만, `${}`는 문자 그대로 치환한다. 컬럼명을 동적으로 넣는 등 바인딩이 불가능한 경우에만 쓰되, **SQL 인젝션 공격에 노출되므로 가급적 사용하지 않는다.**
:::

컬럼명과 프로퍼티명이 다를 때는 별칭(`as`) 외에 `resultMap`으로 매핑을 선언할 수 있다.

```xml
<resultMap id="userResultMap" type="User">
    <id property="id" column="user_id" />
    <result property="username" column="user_name"/>
</resultMap>
```

:::tip 복잡한 연관관계 매핑은 신중히
MyBatis도 `<association>`, `<collection>`으로 객체 연관관계 매핑이 가능하지만, 공수가 많고 성능 최적화가 어렵다. 이런 객체-관계 매핑은 ORM인 JPA가 훨씬 자연스럽다. MyBatis에서는 신중하게 사용한다.
:::

### 객체 연관관계 매핑

객체끼리 **참조로 연결된 관계**를 DB 테이블의 **외래키 관계**와 맞춰 데이터를 채워주는 것. DB는 테이블끼리 외래키 숫자값으로 연결(`post.user_id → user.id`)하지만, 객체 세계에서는 숫자가 아니라 객체 참조로 표현하고 싶다(`Post.author`에 User 객체 자체를 담음).

```java
class Post {
    Long id;
    String title;
    User author;   // user_id 숫자가 아니라, User 객체 자체를 들고 있고 싶음
}

class User {
    Long id;
    String name;
}
```

이 **"숫자 외래키 ↔ 객체 참조" 변환**이 연관관계 매핑이다. DB로 post를 조회하면 `user_id = 5` 같은 숫자만 오므로, user 테이블에서 5번을 다시 조회해 User 객체를 만들고 `Post.author`에 꽂아주는 작업이 필요하다.

MyBatis에서는 `<association>`(다대일·1:1), `<collection>`(1:다) 태그로 가능하지만 관계가 복잡해지면 XML 설정이 장황하고 성능 최적화가 어려워 신중히 써야 한다. 반면 JPA는 ORM의 본질이라 `@ManyToOne` + `@JoinColumn` 선언만으로 처리되어 자연스럽다.

```java
@ManyToOne
@JoinColumn(name = "user_id")
private User author;   // post.getAuthor() 호출 시 JPA가 알아서 user를 조회해 채워줌
```

:::tip 깊이는 JPA에서
연관관계 매핑은 JPA 학습의 핵심 산이다. `@ManyToOne`/`@OneToMany`, 단방향·양방향, 지연로딩, N+1 문제가 전부 여기서 파생된다. 지금은 "객체 참조 ↔ 테이블 외래키를 연결하는 것"이라는 개념만 잡고, MyBatis에선 잘 안 쓴다 정도로 넘어가면 된다. 본격 학습은 JPA 챕터에서.
:::

## 데이터 접근 기술 - JPA

### JPA 개요

JPA는 ORM 데이터 접근 기술이며, 자바 표준 인터페이스이다. JdbcTemplate·MyBatis 같은 SQL 매퍼는 SQL을 개발자가 직접 작성하지만, **JPA는 SQL을 대신 작성·실행**해준다. 글로벌에서는 스프링+JPA 조합을 80% 이상, 국내도 50%가량 쓰며 추세가 늘고 있다.

ORM(Object-Relational Mapping)은 자바 객체와 관계형 데이터베이스(테이블)를 매핑해주는 기술이다.

실무에서는 JPA를 더 편하게 쓰려고 **스프링 데이터 JPA**와 **Querydsl**을 함께 쓴다. 둘은 JPA를 편리하게 쓰게 돕는 도구이고, 본질은 JPA다.

### 설정

`spring-boot-starter-data-jpa` 의존성을 추가한다. 이 스타터가 `spring-boot-starter-jdbc`를 포함하므로 기존 jdbc 의존성은 제거해도 된다.

```groovy
implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
```

포함되는 핵심 라이브러리는 `hibernate-core`(JPA 구현체), `jakarta.persistence-api`(JPA 인터페이스), `spring-data-jpa`다. JPA는 인터페이스(표준)고, **하이버네이트가 실제 구현체**라는 점이 중요하다.

SQL 로그 설정(부트 3.0 / 하이버네이트 6 기준):

```properties
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
```

:::warning show-sql은 비권장
`spring.jpa.show-sql=true`는 System.out으로 출력돼서 권장하지 않는다. logger 방식(위 설정)을 쓰고, 둘 다 켜면 로그가 중복된다.
:::

### 엔티티 매핑 (가장 중요)

JPA의 핵심은 객체와 테이블을 매핑하는 것. 애노테이션으로 선언한다.

```java
@Data
@Entity
public class Item {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "item_name", length = 10)
    private String itemName;
    private Integer price;
    private Integer quantity;

    public Item() {}   // 기본 생성자 필수
}
```

-   `@Entity`: JPA가 관리하는 객체(엔티티)임을 표시. 이게 있어야 JPA가 인식
-   `@Id`: 테이블 PK와 필드를 매핑
-   `@GeneratedValue(strategy = IDENTITY)`: PK를 DB가 생성(MySQL auto increment 등)
-   `@Column(name="item_name", length=10)`: 필드↔컬럼 매핑. `length`는 DDL 자동 생성 시 컬럼 길이로 활용
-   스프링 부트 통합 시 카멜→스네이크 자동 변환(`itemName`→`item_name`)이라 `@Column(name=...)`은 생략 가능

:::warning 기본 생성자 필수
JPA는 `public` 또는 `protected` 기본 생성자가 반드시 필요하다. 빼먹으면 동작하지 않는다.
:::

### EntityManager와 기본 CRUD

JPA의 모든 동작은 **EntityManager**를 통해 이뤄진다. 스프링이 주입해주며, 내부에 데이터소스를 갖고 DB에 접근한다.

```java
@Repository
@Transactional
public class JpaItemRepositoryV1 implements ItemRepository {
    private final EntityManager em;   // 생성자 주입
}
```

:::tip 변경에는 트랜잭션 필수
JPA의 모든 데이터 변경(등록·수정·삭제)은 트랜잭션 안에서 이뤄져야 한다(조회는 트랜잭션 없이도 가능). 일반적으로는 **서비스 계층에 트랜잭션을 거는 것이 맞다.** 이 예제는 비즈니스 로직이 없어 편의상 리포지토리에 걸었을 뿐이다.
:::

저장 — `em.persist(item)`. PK가 IDENTITY라 INSERT SQL에는 id가 빠지고, 실행 후 DB가 생성한 PK를 JPA가 객체 id에 채워준다.

```java
public Item save(Item item) {
    em.persist(item);
    return item;
}
```

조회 — `em.find(타입, PK)`. JPA가 SELECT를 만들어 실행하고 결과를 객체로 변환.

```java
Item item = em.find(Item.class, id);
```

### 변경 감지 (dirty checking) — JPA의 핵심 동작

수정 코드를 보면 `em.update()` 같은 호출이 **전혀 없는데** UPDATE SQL이 나간다.

```java
public void update(Long itemId, ItemUpdateDto updateParam) {
    Item findItem = em.find(Item.class, itemId);
    findItem.setItemName(updateParam.getItemName());
    findItem.setPrice(updateParam.getPrice());
    findItem.setQuantity(updateParam.getQuantity());
    // update 호출 없음! 그래도 UPDATE SQL 실행됨
}
```

JPA는 **트랜잭션 커밋 시점에 변경된 엔티티가 있는지 확인**하고, 변경됐으면 자동으로 UPDATE SQL을 실행한다. 이 원리를 정확히 이해하려면 영속성 컨텍스트를 알아야 한다.

:::tip 테스트에서 UPDATE가 안 보이는 이유
테스트는 끝에 트랜잭션이 롤백되므로 커밋 시점이 없어 UPDATE SQL이 실행되지 않는다. 확인하려면 `@Commit`을 붙인다.
:::

### JPQL과 동적 쿼리 문제

PK 단건 조회가 아니라 복잡한 조건 조회는 **JPQL**(Java Persistence Query Language)을 쓴다. JPQL(Java Persistence Query Language)은 JPA에서 제공하는 객체 지향 쿼리 언어이다.

```java
String jpql = "select i from Item i where i.itemName like concat('%',:itemName,'%')";
TypedQuery<Item> query = em.createQuery(jpql, Item.class);
query.setParameter("itemName", itemName);
return query.getResultList();
```

-   SQL은 **테이블** 대상, JPQL은 **엔티티 객체** 대상. `from Item`의 Item은 테이블이 아니라 엔티티 이름이고, 대소문자를 구분한다.
-   문법은 SQL과 거의 같아 적응이 쉽다
-   파라미터는 `:이름`으로 쓰고 `setParameter`로 바인딩
-   실행되면 엔티티 매핑 정보를 활용해 실제 SQL로 변환된다

:::warning 동적 쿼리는 JPA의 약점
JPQL도 결국 문자열 더하기로 동적 쿼리를 짜야 해서 JdbcTemplate과 같은 불편함이 남는다. 실무에서는 이 문제 때문에 **Querydsl**을 함께 쓴다.
:::

### 예외 변환 — @Repository의 역할

`EntityManager`는 순수 JPA 기술이라 예외도 JPA 예외(`PersistenceException` 및 하위, `IllegalStateException`, `IllegalArgumentException`)를 던진다. 이를 스프링 예외 추상화(`DataAccessException`)로 바꿔주는 비밀이 **`@Repository`**다.

`@Repository`가 붙으면 컴포넌트 스캔 대상이 되는 동시에, **예외 변환 AOP의 대상**이 된다. 스프링+JPA를 함께 쓰면 스프링이 JPA 예외 변환기(`PersistenceExceptionTranslator`)를 등록하고, AOP 프록시가 JPA 예외를 가로채 스프링 데이터 접근 예외로 변환한다.

:::tip
리포지토리에 `@Repository`만 붙이면 스프링이 예외 변환 AOP를 자동으로 만들어준다. 부트가 `PersistenceExceptionTranslationPostProcessor`를 자동 등록하면서 처리되는 것이다.
:::

## 데이터 접근 기술 - 스프링 데이터 JPA

### 개요

스프링 데이터 JPA는 JPA를 **편리하게 쓰도록 도와주는 라이브러리**다. JPA를 직접 쓸 때 반복되는 코드를 줄여준다. 대표 기능은 두 가지다.

-   **공통 인터페이스 기능**: 기본 CRUD를 제공
-   **쿼리 메서드 기능**: 메서드 이름만으로 쿼리 자동 생성

:::warning 본질은 여전히 JPA
스프링 데이터 JPA는 어디까지나 JPA를 편하게 쓰는 도구다. 메서드 이름으로 만들어진 것도 결국 JPQL → SQL로 번역돼 실행된다. 따라서 **JPA 자체를 잘 이해하는 것이 가장 중요**하다.
:::

### 공통 인터페이스 — JpaRepository

`JpaRepository`를 상속하고 제네릭에 `<엔티티, ID타입>`만 주면 기본 CRUD가 전부 제공된다.

```java
public interface ItemRepository extends JpaRepository<Item, Long> {
}
```

`JpaRepository`는 `Repository ← CrudRepository ← PagingAndSortingRepository ← JpaRepository` 계층으로, `save`, `findById`, `findAll`, `delete`, `count`, 페이징·정렬까지 공통화 가능한 기능이 거의 다 들어있다.

:::tip 구현체 없이 동작하는 원리
인터페이스만 상속하면 스프링 데이터 JPA가 **프록시 기술로 구현 클래스를 자동 생성**하고 스프링 빈으로 등록한다. 개발자는 구현체를 안 짜도 된다. 이건 앞서 MyBatis의 `@Mapper` 프록시 자동 생성과 **정확히 같은 메커니즘**이다.
:::

### 쿼리 메서드 — 메서드 이름으로 쿼리 생성

공통 CRUD로 안 되는 검색(이름으로, 가격으로 등)은 **메서드 이름을 규칙대로 적으면** 스프링 데이터 JPA가 분석해서 JPQL을 만들어 실행해준다.

```java
List<Item> findByItemNameLike(String itemName);
List<Item> findByPriceLessThanEqual(Integer price);
List<Item> findByItemNameLikeAndPriceLessThanEqual(String itemName, Integer price);
```

주요 명명 규칙:

-   조회: `find…By`, `read…By`, `query…By`, `get…By`
-   COUNT: `count…By` (반환 long), EXISTS: `exists…By` (반환 boolean)
-   삭제: `delete…By`, `remove…By`
-   DISTINCT: `findDistinct…`, LIMIT: `findFirst3`, `findTop`, `findTop3`
-   조건 결합: `And`, 비교: `LessThanEqual`, `GreaterThan` 등

### @Query — JPQL 직접 작성

메서드 이름 방식의 한계가 있다. (1) 조건이 많으면 메서드 이름이 너무 길어지고, (2) 조인 같은 복잡한 조건을 못 쓴다. 이럴 땐 `@Query`로 JPQL을 직접 작성한다.

```java
@Query("select i from Item i where i.itemName like :itemName and i.price <= :price")
List<Item> findItems(@Param("itemName") String itemName, @Param("price") Integer price);
```

-   `@Query`를 쓰면 메서드 이름 규칙은 무시된다
-   메서드 이름 방식은 파라미터를 순서대로 받지만, `@Query`는 **`@Param`으로 명시적 바인딩**이 필요하다
-   네이티브 쿼리(SQL 직접 작성)도 지원한다

:::warning 동적 쿼리에 약하다
스프링 데이터 JPA는 동적 쿼리 지원이 매우 약하다. 예제의 `findAll`도 조건 조합(이름만/가격만/둘다/없음)을 if-else로 분기해 각각 다른 메서드를 호출하는 비효율적 코드다. `Example` 기능이 있지만 실무에선 빈약하다. **동적 쿼리는 이후 Querydsl로 해결**한다.
:::

### 어댑터 구조 (실무 설계 포인트)

`ItemService`는 처음부터 `ItemRepository` 인터페이스에 의존하도록 작성돼 있다(기술 중립적 명세). 그런데 스프링 데이터 JPA가 만들어주는 `SpringDataJpaItemRepository`는 `JpaRepository`를 상속한 **별도 인터페이스**라 타입도 메서드 모양도 `ItemRepository`와 다르다. 그래서 서비스에 그대로 끼울 수 없다.

선택지는 둘이다. (1) `ItemService`가 `SpringDataJpaItemRepository`를 직접 쓰도록 고친다 → 서비스가 특정 기술에 종속된다. (2) 둘 사이에 타입을 맞춰주는 **어댑터**를 둔다 → 서비스 코드를 안 건드린다. 강의는 (2)를 택했고, 그 어댑터가 `JpaItemRepositoryV2`다.

```java
@Repository
@Transactional
@RequiredArgsConstructor
public class JpaItemRepositoryV2 implements ItemRepository {  // 서비스가 원하는 타입
    private final SpringDataJpaItemRepository repository;     // 실제 일꾼(프록시) 주입

    public Item save(Item item) {
        return repository.save(item);                 // 순수 위임
    }
    public Optional<Item> findById(Long id) {
        return repository.findById(id);               // 순수 위임
    }
    public void update(Long itemId, ItemUpdateDto updateParam) {
        Item findItem = repository.findById(itemId).orElseThrow();
        findItem.setItemName(updateParam.getItemName());   // 조회 후 값 변경 → 변경 감지로 UPDATE
        findItem.setPrice(updateParam.getPrice());
        findItem.setQuantity(updateParam.getQuantity());
    }
    public List<Item> findAll(ItemSearchCond cond) {        // 자체 변환 로직(분기 + 와일드카드)
        // 조건 조합에 따라 어떤 쿼리 메서드를 부를지 직접 분기
    }
}
```

### 등장인물과 책임 분리

세 타입의 역할을 명확히 나눠야 이 구조가 보인다.

-   **`ItemRepository`**: `ItemService`가 의존하는 인터페이스. 데이터 접근 명세만 정의(기술 중립)
-   **`SpringDataJpaItemRepository`**: 내가 선언한 인터페이스이며 `JpaRepository<Item, Long>`을 상속. **실제 쿼리를 정의하는 곳**(쿼리 메서드 이름 규칙 또는 `@Query`). 구현 클래스는 직접 안 짠다
-   **`JpaItemRepositoryV2`**: `ItemRepository`를 구현한 **어댑터**. 위 둘의 간극을 메운다

:::tip 구현체 없이 동작하는 원리
`SpringDataJpaItemRepository`는 인터페이스일 뿐인데, 스프링 데이터 JPA가 로딩 시점에 **동적 프록시로 구현체를 자동 생성**해 스프링 빈으로 등록한다. 그래서 어댑터는 이 프록시 객체를 주입받아 쓸 수 있다. (앞서 MyBatis `@Mapper`가 프록시로 구현되던 것과 동일한 메커니즘)
:::

### 런타임 의존 구조

```text
itemService → jpaItemRepositoryV2 → springDataJpaItemRepository(프록시 객체)
```

호출 흐름으로 읽으면, `itemService`가 리포지토리 메서드를 호출(대상은 `ItemRepository` 타입으로 주입된 어댑터) → 어댑터가 주입받은 `SpringDataJpaItemRepository` 프록시에 위임 → 프록시가 실제 쿼리를 생성·실행한다.

이때 주입되는 객체들은 모두 **스프링 빈**(기본 싱글턴 스코프)이다. "객체가 하나뿐"이라는 점보다, **스프링 컨테이너가 관리하고 DI로 주입한다**는 점이 핵심이다. 덕분에 구현 기술을 JdbcTemplate → MyBatis → JPA → 스프링 데이터 JPA로 바꿔도 `ItemService` 코드는 한 줄도 안 바뀌고, 바뀌는 건 어떤 구현체를 주입할지 정하는 **Config 설정뿐**이다.

### 쿼리 수행 주체는 어댑터가 아니다

어댑터는 **스스로 쿼리를 만들지 않는다.** "어떤 메서드를 부를지" 고르고 위임할 뿐이다. 실제 쿼리는 `SpringDataJpaItemRepository`에 정의돼 있어야 한다.

```java
// 실제 쿼리 정의는 이 인터페이스에 (메서드 이름 규칙 또는 @Query)
public interface SpringDataJpaItemRepository extends JpaRepository<Item, Long> {
    List<Item> findByItemNameLike(String itemName);   // 이름 규칙으로 쿼리 생성

    @Query("select i from Item i where i.itemName like :itemName and i.price <= :price")
    List<Item> findItems(@Param("itemName") String itemName, @Param("price") Integer price);
}
```

수행을 두 겹으로 구분하면 정확하다. **쿼리 정의·생성**은 `SpringDataJpaItemRepository`(+그 프록시)가, **실제 SQL 실행**은 그 아래 JPA(하이버네이트) → JDBC → DB가 담당한다.

### 어댑터 메서드의 두 종류

어댑터 메서드가 전부 단순 위임인 것은 아니다. 두 성격이 공존한다.

-   **순수 위임**(`save`, `findById`): 받아서 그대로 넘김
-   **자체 변환 로직**(`findAll`, `update`): 두 인터페이스의 간극을 메우는 로직을 직접 가짐. `findAll`은 검색 조건 조합(이름만/가격만/둘 다/없음)에 따라 호출할 메서드를 분기하고 `%` 와일드카드를 붙인다

이 변환 로직, 특히 `findAll`의 분기야말로 어댑터가 한 겹 존재하는 실질적 이유다. 단순 위임만이라면 어댑터의 값어치가 작지만, 명세 차이를 메우는 이 부분이 있어 한 겹이 의미를 갖는다.

:::tip update에 update 호출이 없는 이유
`update()`는 `findById`로 엔티티를 조회한 뒤 setter만 호출한다. 저장 메서드 호출이 없는데도 트랜잭션 커밋 시점에 **변경 감지(dirty checking)** 로 UPDATE SQL이 나간다. 앞 JPA 챕터의 동작 그대로이며, 영속성 컨텍스트가 관리하는 엔티티의 변경을 추적하기 때문이다.
:::

:::warning 실무에서 항상 어댑터를 두는 건 아니다
이 어댑터 구조는 "서비스가 기술 중립적 `ItemRepository`에 의존한다"는 전제에서 나온 설계다. 실무에서는 `SpringDataJpaItemRepository`(JpaRepository 상속 인터페이스)를 서비스가 **직접 사용**하는 경우도 많다. 추가 추상화 계층 없이 스프링 데이터 JPA에 의존하기로 결정했다면 어댑터가 불필요한 군더더기가 될 수 있다. 어댑터는 "구현 기술을 자유롭게 갈아끼울 여지를 남긴다"는 이점과 "계층이 하나 늘어난다"는 비용의 트레이드오프다.
:::

### 예외 변환

스프링 데이터 JPA가 만들어주는 **프록시에서 이미 예외 변환을 처리**한다. 그래서 JPA 챕터와 달리 `@Repository`가 없어도 스프링 예외 추상화(`DataAccessException`)로 변환된다.

### 스프링 데이터 JPA의 역할

스프링 데이터 JPA를 "동적 프록시 제공 역할만 한다"고 보는 건 절반만 맞다. 동적 프록시는 **구현 클래스 없이 인터페이스만으로 쓰게 해주는 전달 메커니즘**일 뿐이고, 실제 가치는 그 프록시에 담아 제공하는 **내용물**에 있다.

구체적으로는 `JpaRepository` 상속만으로 주어지는 공통 CRUD 구현(`save`, `findById`, `findAll`, `delete`, `count`), 메서드 이름을 파싱해 JPQL을 만들어주는 쿼리 메서드 생성, `@Query` 처리, 페이징·정렬(`Pageable`, `Sort`), 그리고 예외 변환(`@Repository` 없이도 JPA 예외 → 스프링 예외 추상화)까지를 묶어서 제공한다.

즉 프록시는 "수단", 위 기능들이 "내용"이라 하나로 뭉뚱그리면 안 된다. 다만 **스프링 데이터 JPA가 스스로 DB 작업을 하지는 않는다**는 이해는 정확하다. 영속성 컨텍스트 관리, SQL 생성·실행 같은 본질은 전부 아래 계층의 JPA(하이버네이트)가 수행하고, 스프링 데이터 JPA는 그 JPA 호출을 대신 작성해주고 인터페이스만으로 쓰게 포장·자동화하는 편의 계층이다.

:::tip 한 줄 정리
프록시 = 전달 메커니즘, 실제 가치 = 공통 CRUD + 쿼리 메서드 생성 + 페이징 + 예외 변환. 단, 실제 DB 작업은 JPA에 위임하며 스스로 하지 않는다.
:::

## Querydsl

### Querydsl이 해결하는 문제

기존 JPA(JPQL) 기반 동적 쿼리는 문자열로 쿼리를 조립해야 해서 오타에 취약하고, 오류를 런타임에야 발견한다. Querydsl은 자바 코드로 쿼리를 작성하므로 컴파일 시점에 오류를 잡고, 조건을 메서드로 모듈화해 재사용할 수 있다.

JPQL의 type-unsafe 특성을 해소하기 위한 프레임워크이기 때문에, QueryDSL을 잘 쓰기 위해서는 JPQL을 잘 알아야 한다. QueryDSL은 JPQL 빌더로서 동작한다.

### 설정 (스프링 부트 3.x)

스프링 부트 버전에 따라 설정이 다르며, 실무 기준 3.x 설정만 정리한다.

```groovy
dependencies {
    // Querydsl 추가
    implementation 'com.querydsl:querydsl-jpa:5.0.0:jakarta'
    annotationProcessor "com.querydsl:querydsl-apt:${ dependencyManagement.importedProperties['querydsl.version'] }:jakarta"
    annotationProcessor "jakarta.annotation:jakarta.annotation-api"
    annotationProcessor "jakarta.persistence:jakarta.persistence-api"
}

// 자동 생성된 Q클래스를 gradle clean으로 제거
clean {
    delete file('src/main/generated')
}
```

2.x와 비교하면 `jpa` → `jakarta`로 바뀐 점이 핵심이다.

:::tip 버전 의존성
IntelliJ나 Querydsl Gradle 설정은 버전업 때마다 적용 방법이 조금씩 달라진다. 환경에 따라 동작이 다를 수 있으니, 잘 안 되면 `querydsl gradle`로 검색해 본인 환경에 맞는 대안을 찾으면 된다.
:::

### Q타입 생성 및 확인

Q타입은 Querydsl이 컴파일 시점에 자동으로 만들어주는 쿼리 전용 클래스이다.

빌드 방식 옵션(`Build and run using`, `Run tests using`)은 Gradle / IntelliJ IDEA 둘 다 같게 맞춘다.

-   Gradle 옵션: `build -> generated -> sources -> annotationProcessor -> java/main` 하위에 Q타입 생성 (예: `QItem`)
-   IntelliJ IDEA 옵션: `src/main/generated` 하위에 생성
-   콘솔: `./gradlew clean compileJava`

:::warning Q타입과 버전관리
Q타입은 컴파일 시점에 자동 생성되므로 Git에 포함하지 않는다. Gradle 옵션은 build 폴더에 생성되어 자연스럽게 제외되지만, IntelliJ IDEA 옵션은 `src/main/generated`에 생성되므로 `.gitignore` 처리 및 `clean` 스크립트가 필요하다.
:::

### 적용 - JPAQueryFactory

Querydsl을 쓰려면 `JPAQueryFactory`가 필요하고, JPQL을 만들기 위해 `EntityManager`가 필요하다. 스프링 빈으로 등록해서 써도 된다.

```java
@Repository
@Transactional
public class JpaItemRepositoryV3 implements ItemRepository {

    private final EntityManager em;
    private final JPAQueryFactory query;

    public JpaItemRepositoryV3(EntityManager em) {
        this.em = em;
        this.query = new JPAQueryFactory(em);
    }
}
```

`save()`, `update()`, `findById()` 같은 기본 기능은 JPA 기본 기능을 그대로 사용한다.

### 동적 쿼리 - BooleanBuilder

여러 조건을 `BooleanBuilder`에 누적해서 `where`에 넘기는 방식.

```java
QItem item = QItem.item;
BooleanBuilder builder = new BooleanBuilder();

if (StringUtils.hasText(itemName)) {
    builder.and(item.itemName.like("%" + itemName + "%"));
}
if (maxPrice != null) {
    builder.and(item.price.loe(maxPrice));
}

List<Item> result = query
        .select(item)
        .from(item)
        .where(builder)
        .fetch();
```

### 동적 쿼리 - Where 다중 파라미터 (권장)

실무에서 더 깔끔한 방식. 조건을 `BooleanExpression` 반환 메서드로 분리하고 `where(A, B)`에 나열한다.

```java
public List<Item> findAll(ItemSearchCond cond) {
    String itemName = cond.getItemName();
    Integer maxPrice = cond.getMaxPrice();

    List<Item> result = query
            .select(item)
            .from(item)
            .where(likeItemName(itemName), maxPrice(maxPrice))
            .fetch();
    return result;
}

private BooleanExpression likeItemName(String itemName) {
    if (StringUtils.hasText(itemName)) {
        return item.itemName.like("%" + itemName + "%");
    }
    return null;
}

private BooleanExpression maxPrice(Integer maxPrice) {
    if (maxPrice != null) {
        return item.price.loe(maxPrice);
    }
    return null;
}
```

:::tip Where 다중 파라미터 핵심

-   `where(A, B)`에 나열한 조건은 자동으로 **AND**로 묶인다
-   `where()`에 `null`을 넘기면 해당 조건은 **무시**된다
-   `likeItemName()`, `maxPrice()` 같은 조건 메서드를 다른 쿼리에서 **재사용/조합**할 수 있다 (조건 모듈화)

:::

### 설정 등록

```java
@Configuration
@RequiredArgsConstructor
public class QuerydslConfig {

    private final EntityManager em;

    @Bean
    public ItemService itemService() {
        return new ItemServiceV1(itemRepository());
    }

    @Bean
    public ItemRepository itemRepository() {
        return new JpaItemRepositoryV3(em);
    }
}
```

```java
@Import(QuerydslConfig.class)
@SpringBootApplication(scanBasePackages = "hello.itemservice.web")
public class ItemServiceApplication {}
```

### 예외 변환

Querydsl은 별도의 스프링 예외 추상화를 지원하지 않는다. 대신 `@Repository`가 JPA에서처럼 스프링 예외 추상화를 처리해준다.

### Querydsl 장점

-   동적 쿼리를 자바 코드로 매우 깔끔하게 작성
-   쿼리 오타를 **컴파일 시점**에 잡음
-   조건 메서드 추출로 코드 재사용 가능
-   DTO 직접 조회 등 실무에서 자주 쓰는 편의 기능 제공 → 스프링 데이터 JPA + Querydsl 조합이 실무 기본 기술

## 데이터 접근 기술 - 활용 방안

### 스프링 데이터 JPA 예제와 트레이드 오프

기존 구조에서는 `JpaItemRepositoryV2`가 어댑터 역할을 한다. 덕분에 `ItemService`가 의존하는 `ItemRepository` 인터페이스를 그대로 유지할 수 있고, `ItemService` 코드를 바꾸지 않아도 구현체를 교체할 수 있다(DI, OCP 준수).

하지만 단점도 명확하다. 구조를 맞추기 위해 중간에 어댑터가 들어가면서 전체 구조가 복잡해지고 클래스가 많아진다. 개발자는 어댑터와 실제 코드를 모두 만들고 유지보수해야 하는 부담이 생긴다.

반대 선택지도 있다. `ItemService`가 스프링 데이터 JPA 리포지토리를 직접 참조하는 방식이다. 이 경우 `ItemService` 코드를 변경해야 하지만, 어댑터를 제거하고 구조를 단순하게 가져갈 수 있다.

:::tip 핵심 - 추상화도 비용이다
트레이드 오프의 본질은 **구조의 안정성 vs 단순한 구조와 개발 편의성**이다. 추상화와 인터페이스 도입에는 유지보수 비용이 따른다. 그 비용을 넘어설 만큼 효과가 있을 때 추상화를 도입하는 것이 실용적이다. 어설픈 추상화는 오히려 독이 된다. 정답은 없고, 현재 프로젝트 상황에 맞는 선택이 있을 뿐이다.
:::

### 실용적인 구조 - 복잡한 쿼리 분리

스프링 데이터 JPA의 기능을 최대한 살리면서 Querydsl도 편리하게 쓰는 구조. 리포지토리를 역할에 따라 둘로 나눈다.

기본 CRUD·단순 조회는 스프링 데이터 JPA(`ItemRepositoryV2`)가 담당하고, 복잡한 조회 쿼리는 Querydsl(`ItemQueryRepositoryV2`)이 담당한다.

스프링 데이터 JPA 리포지토리는 `JpaRepository`를 상속만 하면 된다.

```java
public interface ItemRepositoryV2 extends JpaRepository<Item, Long> {
}
```

Querydsl 리포지토리는 일반 `@Repository`로 만들고 `JPAQueryFactory`를 사용한다. 복잡한 쿼리가 이 클래스에 모이므로 유지보수 지점이 명확해진다.

```java
@Repository
public class ItemQueryRepositoryV2 {

    private final JPAQueryFactory query;

    public ItemQueryRepositoryV2(EntityManager em) {
        this.query = new JPAQueryFactory(em);
    }

    public List<Item> findAll(ItemSearchCond cond) {
        return query.select(item)
                .from(item)
                .where(maxPrice(cond.getMaxPrice()),
                       likeItemName(cond.getItemName()))
                .fetch();
    }
    // likeItemName(), maxPrice() : 조건이 null이면 무시됨
}
```

서비스는 두 리포지토리를 모두 주입받아 역할에 맞게 호출한다.

```java
@Service
@RequiredArgsConstructor
@Transactional
public class ItemServiceV2 implements ItemService {

    private final ItemRepositoryV2 itemRepositoryV2;          // 스프링 데이터 JPA
    private final ItemQueryRepositoryV2 itemQueryRepositoryV2; // Querydsl

    @Override
    public Item save(Item item) {
        return itemRepositoryV2.save(item);
    }

    @Override
    public List<Item> findItems(ItemSearchCond cond) {
        return itemQueryRepositoryV2.findAll(cond);  // 복잡한 조회는 Querydsl로
    }
    // update, findById 등은 itemRepositoryV2 사용
}
```

:::tip
스프링 데이터 JPA가 제공하는 커스텀 리포지토리 기능을 써도 비슷하게 해결할 수 있다.
:::

### 다양한 데이터 접근 기술 조합

기술 선택에 정답은 없고, 비즈니스 상황과 팀 구성원의 역량에 따라 결정한다. JdbcTemplate·MyBatis는 SQL을 직접 써야 하지만 단순해서 SQL에 익숙하면 금방 적응한다. JPA·스프링 데이터 JPA·Querydsl은 생산성이 높지만 학습 곡선이 가파르고, 매우 복잡한 통계 쿼리에는 잘 맞지 않는다.

권장 방향은 JPA·스프링 데이터 JPA·Querydsl을 기본으로 쓰고, 복잡한 쿼리가 잘 안 풀리는 부분만 JdbcTemplate·MyBatis를 함께 쓰는 것이다. 실무에서 대략 95%는 JPA 계열로, 나머지 5%는 직접 SQL로 해결한다는 비유가 나온다(프로젝트마다 다름).

### 트랜잭션 매니저 선택과 조합

JPA·스프링 데이터 JPA·Querydsl은 모두 JPA 기술이므로 `JpaTransactionManager`를 쓰고, 스프링 부트가 자동 등록해준다. JdbcTemplate·MyBatis는 내부에서 JDBC를 쓰므로 `DataSourceTransactionManager`를 쓴다.

:::tip JpaTransactionManager 하나로 묶기
`JpaTransactionManager`는 `DataSourceTransactionManager`의 기능도 대부분 제공한다(JPA도 내부적으로 DataSource와 JDBC 커넥션을 쓰기 때문). 따라서 `JpaTransactionManager` 하나만 빈으로 등록하면 JPA, JdbcTemplate, MyBatis를 **하나의 트랜잭션으로 묶고 함께 롤백**할 수 있다.
:::

:::warning JPA + JdbcTemplate 함께 쓸 때 플러시 주의
JPA는 변경 사항을 즉시 DB에 반영하지 않고 기본적으로 트랜잭션 커밋 시점에 반영한다. 그래서 같은 트랜잭션 안에서 JPA로 데이터를 변경한 뒤 JdbcTemplate을 호출하면, JdbcTemplate이 JPA의 변경분을 읽지 못한다. JPA 호출이 끝난 시점에 `flush()`로 변경 내역을 DB에 반영해야 그다음 JdbcTemplate이 그 데이터를 사용할 수 있다.
:::

### JDBC 커넥션과 트랜잭션 동기화 매니저

JDBC 커넥션(`java.sql.Connection`)은 자바 애플리케이션과 DB를 잇는 통로다. 트랜잭션은 이 커넥션 단위로 동작한다 — `con.setAutoCommit(false)`로 시작하고 `con.commit()` / `con.rollback()`으로 끝내는 일이 모두 한 `Connection` 객체에서 일어나므로, 같은 트랜잭션을 유지하려면 같은 커넥션을 써야 한다. JPA·JdbcTemplate·MyBatis 모두 내부에서는 결국 이 커넥션을 얻어 쓴다.

커넥션 객체의 생명주기는 애플리케이션보다 훨씬 짧아 보통 트랜잭션(요청) 단위로 살았다 사라진다. 단, "커넥션 객체"와 "물리적 연결"은 다르다. 커넥션 풀(HikariCP 등)을 쓰면 `con.close()`는 연결을 끊는 게 아니라 풀에 반납하는 것이다. 풀과 물리적 연결은 앱과 생명주기가 거의 같고(앱 시작 시 생성, 종료 시 정리), 코드가 빌려 쓰는 커넥션 핸들만 요청마다 짧게 살았다 죽는다.

서비스 메서드 하나에서 여러 리포지토리를 호출할 때, 쿼리들이 원자성을 가지려면 같은 커넥션에 묶여야 한다. 커넥션을 파라미터로 일일이 넘기는 건 번거롭기 때문에, 스프링은 트랜잭션 동기화 매니저에 커넥션을 보관해 공유한다. 보관 메커니즘은 `ThreadLocal`(쓰레드마다 독립된 보관함)이다. 하나의 요청을 하나의 쓰레드가 처리하므로, 현재 쓰레드에 커넥션을 묶어두면 그 요청 동안 어디서 꺼내도 같은 커넥션이 나온다.

```java
// 트랜잭션 시작 시 (트랜잭션 매니저가 수행)
con.setAutoCommit(false);
TransactionSynchronizationManager.bindResource(dataSource, con);  // 현재 쓰레드에 보관
// 종료 시: commit/rollback → unbindResource → con.close()(풀 반납)
```

이 보관·해제 경계를 만드는 것이 `@Transactional`이다. 트랜잭션 프록시(AOP)가 메서드 진입 직전 커넥션을 얻어 bind하고, 정상 종료 시 commit / 예외 시 rollback 후 unbind한다. 메서드 안에서 호출된 리포지토리들은 직접 `ThreadLocal`을 뒤지지 않고, 각 기술이 `DataSourceUtils`를 거쳐 동기화 매니저를 조회한다 — 트랜잭션이 있으면 보관된 커넥션을 재사용하고, 없으면 새 커넥션을 얻는다. 그 결과 한 트랜잭션 안의 모든 쿼리가 같은 커넥션으로 묶여 원자성을 갖는다.

:::tip 같은 쓰레드에 트랜잭션이 여러 개라면
쓰레드가 다르면 트랜잭션은 완전히 분리된다(`ThreadLocal`). 같은 쓰레드 안에서 `@Transactional`이 중첩되면 전파(propagation) 옵션이 결정한다. 기본값 `REQUIRED`는 바깥 트랜잭션에 참여(커넥션 하나 공유), `REQUIRES_NEW`는 바깥 트랜잭션을 잠시 대피(suspend)시키고 새 커넥션으로 별도 트랜잭션을 시작한 뒤 끝나면 복구(resume)한다. 어느 순간이든 활성 상태로 보관함을 점유하는 트랜잭션은 하나뿐이다.
:::

### 정리

`ItemServiceV2`는 스프링 데이터 JPA(`ItemRepositoryV2`)와 Querydsl(`ItemQueryRepositoryV2`)을 직접 참조해, 기본 기능은 데이터 JPA로 활용하고 복잡한 쿼리는 Querydsl로 해결한다. 구조가 단순해 프로젝트 규모가 작거나 속도·프로토타입이 중요한 단계에 적합하다. 다만 리포지토리 구현 기술이 바뀌면 많은 코드를 함께 변경해야 하는 단점이 있다. 결국 트레이드 오프를 이해하고 상황에 맞는 선택을 하는 것이 중요하다.

## 스프링 트랜잭션 이해

### 트랜잭션 추상화

JDBC와 JPA는 트랜잭션을 다루는 코드가 서로 다르다. 그래서 데이터 접근 기술을 바꾸면 트랜잭션 코드도 전부 바꿔야 한다. 스프링은 이를 `PlatformTransactionManager` 인터페이스로 추상화해 해결한다.

```java
public interface PlatformTransactionManager extends TransactionManager {
    TransactionStatus getTransaction(TransactionDefinition definition);
    void commit(TransactionStatus status);
    void rollback(TransactionStatus status);
}
```

트랜잭션은 시작(획득)·커밋·롤백으로 단순화된다. 스프링 부트는 사용하는 기술을 자동 인식해 적절한 구현체를 빈으로 등록한다 — JdbcTemplate·MyBatis면 `DataSourceTransactionManager`, JPA면 `JpaTransactionManager`.

### 선언적 트랜잭션과 프록시 AOP

`@Transactional`만 붙여 트랜잭션을 적용하는 것이 선언적 트랜잭션 관리이고, 실무는 대부분 이 방식을 쓴다. `@Transactional`을 쓰면 프록시 방식 AOP가 적용된다. `@Transactional`이 하나라도 있으면 스프링은 실제 객체 대신 CGLIB 프록시를 빈으로 등록하고, 주입 시에도 프록시가 주입된다. 프록시가 트랜잭션을 시작·종료하고, 그 사이에 실제 객체를 호출한다. 덕분에 서비스 계층에는 순수 비즈니스 로직만 남는다. (관심사의 분리)

```properties
# 트랜잭션 적용 여부를 로그로 확인
logging.level.org.springframework.transaction.interceptor=TRACE
```

적용 여부는 `TransactionSynchronizationManager.isActualTransactionActive()`로 가장 확실하게 확인할 수 있다(true면 적용 중).

### @Transactional 적용 위치와 우선순위

규칙은 "더 구체적인 것이 높은 우선순위"다. 적용 순서는 클래스 메서드 → 클래스 타입 → 인터페이스 메서드 → 인터페이스 타입 순. 또 클래스에 적용하면 메서드에 자동 적용된다(메서드에 별도 지정이 없으면 클래스 설정을 따름).

:::warning 인터페이스 적용은 피할 것
인터페이스에 `@Transactional`을 두면 AOP 방식에 따라 적용되지 않을 수 있다. 가급적 구체 클래스에 사용한다.
:::

### 프록시 내부 호출 문제 (실무 필수)

트랜잭션을 적용하려면 항상 프록시를 거쳐 대상 객체를 호출해야 한다. 그런데 대상 객체 내부에서 자기 메서드를 호출하면(`this.internal()`) 프록시를 거치지 않아 트랜잭션이 적용되지 않는다.

프록시를 거쳐 호출한다는 의미는 `@Autowired`와 같이 주입받은 빈을 통해 호출한다는 것을 의미한다. 주입받는 빈 객체 내에 트랜잭션 메서드가 존재해야 한다.

```java
public void external() {   // @Transactional 없음
    internal();            // this.internal() → 프록시 미경유 → 트랜잭션 적용 안 됨
}
@Transactional
public void internal() { ... }
```

external에도 `@Transactional`을 적용하는 경우 internal 내부호출도 트랜잭션으로 처리가 된다. (어떤 트랜잭션으로 처리되는가는 다른 이야기.)

:::tip 해결책
가장 많이 쓰는 방법은 `internal()`을 별도 클래스로 분리해 내부 호출을 외부 호출로 바꾸는 것이다. 그러면 분리된 클래스가 프록시로 주입되어 트랜잭션이 정상 적용된다.
:::

### public 메서드만 적용 / 초기화 시점 주의

스프링 트랜잭션 AOP는 기본적으로 `public` 메서드에만 적용된다(트랜잭션이 의도치 않은 곳까지 과도하게 걸리는 것을 막기 위함). public이 아니면 예외 없이 적용만 무시된다. (스프링 부트 3.0부터는 protected·package-visible에도 적용)

또한 `@PostConstruct` + `@Transactional`을 함께 쓰면 트랜잭션이 적용되지 않는다. 초기화 코드가 트랜잭션 AOP보다 먼저 호출되기 때문이다. 가장 확실한 대안은 `@EventListener(ApplicationReadyEvent.class)`를 쓰는 것으로, 컨테이너가 완전히 뜬 뒤 호출되어 트랜잭션이 적용된다.

### 트랜잭션 옵션

`@Transactional`의 주요 옵션은 다음과 같다. `value`/`transactionManager`(사용할 트랜잭션 매니저 지정, 보통 생략), `rollbackFor`/`noRollbackFor`(롤백 대상 예외 추가·제외 지정), `propagation`(트랜잭션 전파), `isolation`(격리 수준, 기본 DEFAULT는 DB 설정을 따름), `timeout`(타임아웃), `readOnly`(읽기 전용).

:::tip readOnly = true
읽기 전용 트랜잭션을 만든다. JPA(하이버네이트)는 커밋 시점 플러시를 생략하고 변경 감지용 스냅샷도 만들지 않는 등 다양한 최적화가 일어난다. 읽기/쓰기(마스터/슬레이브) DB를 분리한 환경에서는 읽기 DB로 요청을 보내기도 한다.
:::

### 예외와 커밋·롤백

예외가 트랜잭션 범위 밖으로 던져질 때 스프링의 기본 정책은 다음과 같다.

-   언체크 예외(`RuntimeException`, `Error`)와 그 하위 → 롤백
-   체크 예외(`Exception`)와 그 하위 → 커밋
-   정상 리턴 → 커밋

이 정책은 "체크 예외는 비즈니스 의미가 있을 때, 언체크 예외는 복구 불가능한 예외"라는 가정에서 나온다. 기본 정책과 다르게 체크 예외도 롤백하고 싶으면 `rollbackFor`를 쓴다.

```java
@Transactional(rollbackFor = MyException.class)  // 체크 예외인데도 롤백
```

:::tip 비즈니스 예외 활용 예시
주문 시 잔고 부족(`NotEnoughMoneyException`, 체크 예외)이 나면 — 시스템 장애가 아니라 정상 동작 중의 비즈니스 상황이므로 — 주문 데이터는 커밋(저장)하고 결제 상태만 "대기"로 둔 뒤 고객에게 안내한다. 여기서 롤백하면 주문 자체가 사라져 문제가 된다. 반면 시스템 예외(런타임)는 전체 롤백한다.
:::

## 스프링 트랜잭션 전파 - 기본

### 트랜잭션 기본 동작 (커밋 / 롤백)

`PlatformTransactionManager`를 주입받아 트랜잭션을 직접 제어할 수 있다.

```java
TransactionStatus status = txManager.getTransaction(new DefaultTransactionAttribute());
txManager.commit(status);   // 또는 txManager.rollback(status);
```

트랜잭션 시작 시 커넥션 풀에서 커넥션을 획득하고, 종료 시 반납한다.

### 트랜잭션 두 번 사용 (순차 실행)

트랜잭션1이 완전히 끝난 후 트랜잭션2가 시작되면, 둘은 **별개의 트랜잭션**이다.

-   같은 `conn0`이 재사용되어도 `HikariProxyConnection` 객체 주소가 다르면 다른 커넥션으로 봐야 한다
-   각각 독립적으로 커밋/롤백되므로, 트랜잭션1 커밋 + 트랜잭션2 롤백도 가능

### 트랜잭션 전파 기본 개념

이미 트랜잭션이 진행 중일 때 추가 트랜잭션을 시작하면 어떻게 동작할지를 결정하는 것이 **트랜잭션 전파(propagation)**.

기본 옵션은 `REQUIRED`이며, 내부 트랜잭션은 외부 트랜잭션에 **참여**한다.

**물리 트랜잭션 vs 논리 트랜잭션**

| 구분          | 설명                                                               |
| ------------- | ------------------------------------------------------------------ |
| 물리 트랜잭션 | 실제 DB 커넥션에 적용. `setAutoCommit(false)` ~ `commit/rollback`  |
| 논리 트랜잭션 | 트랜잭션 매니저를 통해 사용하는 단위. 중첩 시 각각이 논리 트랜잭션 |

:::warning 핵심 원칙

-   모든 논리 트랜잭션이 커밋되어야 물리 트랜잭션이 커밋된다
-   하나의 논리 트랜잭션이라도 롤백되면 물리 트랜잭션은 롤백된다

:::

### 전파 예제 - 내부 커밋 (inner_commit)

```java
TransactionStatus outer = txManager.getTransaction(new DefaultTransactionAttribute());
// outer.isNewTransaction() == true

TransactionStatus inner = txManager.getTransaction(new DefaultTransactionAttribute());
// inner.isNewTransaction() == false  → 외부 트랜잭션에 참여

txManager.commit(inner); // 논리 커밋만. 물리 커밋 발생 X
txManager.commit(outer); // 신규 트랜잭션이므로 물리 커밋 발생
```

동일한 트랜잭션 매니저로부터 `getTransaction`을 호출하므로 동일한 ThreadLocal로부터 커넥션을 받아오게 된다. inner를 호출하는 단계에서는 이미 outer로부터 생성된 커넥션이 존재하기 때문에 해당 커넥션을 반환받는다.

:::tip
`txManager.commit(inner)`를 호출해도 실제 DB 커밋은 발생하지 않는다.
내부 트랜잭션은 `isNewTransaction == false`이므로, 커밋 요청을 무시하고 외부 트랜잭션이 최종 커밋을 담당한다.
:::

### 전파 예제 - 외부 롤백 (outer_rollback)

내부 트랜잭션이 커밋되어도, 외부 트랜잭션이 롤백하면 **전체가 롤백**된다.

-   내부 트랜잭션은 물리 트랜잭션에 관여하지 않음
-   외부 트랜잭션의 롤백이 물리 롤백으로 이어짐

### 전파 예제 - 내부 롤백 (inner_rollback)

내부 트랜잭션이 롤백하고 외부가 커밋을 시도하는 경우.

```
내부 롤백 시 → rollbackOnly=true 표시
외부 커밋 시 → rollbackOnly 확인 후 물리 롤백 수행
            → UnexpectedRollbackException 예외 발생
```

:::danger UnexpectedRollbackException

외부에서 커밋을 호출했지만 내부 롤백으로 인해 실제로는 롤백된 경우, 스프링은 조용히 넘기지 않고
`UnexpectedRollbackException`을 던진다. 커밋 성공 여부를 명확히 알리기 위한 설계.

외부 트랜잭션 내에 여러개의 내부 트랜잭션이 병렬로 배치되어있을때, 내부 트랜잭션 하나라도 롤백되는 경우 예외를 바로 던져 외부 트랜잭션, 물리 롤백이 완료된 상태가 된다.

:::

### REQUIRES_NEW - 트랜잭션 완전 분리

내부 트랜잭션을 외부와 완전히 분리하고 싶을 때 사용.

```java
DefaultTransactionAttribute definition = new DefaultTransactionAttribute();
definition.setPropagationBehavior(TransactionDefinition.PROPAGATION_REQUIRES_NEW);
TransactionStatus inner = txManager.getTransaction(definition);
```

-   내부 트랜잭션이 **별도의 DB 커넥션**을 새로 획득 (conn1 보류 → conn2 사용)
-   내부 롤백이 외부에 영향 없음. 외부 롤백도 내부에 영향 없음

:::warning
`REQUIRES_NEW`는 DB 커넥션을 동시에 2개 사용한다. 커넥션 풀 고갈 주의.
:::

### 전파 옵션 정리

| 옵션              | 기존 트랜잭션 없음 | 기존 트랜잭션 있음                       |
| ----------------- | ------------------ | ---------------------------------------- |
| `REQUIRED` (기본) | 새로 생성          | 참여                                     |
| `REQUIRES_NEW`    | 새로 생성          | 새로 생성 (기존 보류)                    |
| `SUPPORT`         | 트랜잭션 없이 진행 | 참여                                     |
| `NOT_SUPPORT`     | 트랜잭션 없이 진행 | 트랜잭션 없이 진행 (기존 보류)           |
| `MANDATORY`       | 예외 발생          | 참여                                     |
| `NEVER`           | 트랜잭션 없이 진행 | 예외 발생                                |
| `NESTED`          | 새로 생성          | 중첩 트랜잭션 (JDBC savepoint, JPA 불가) |

:::tip 실무 팁
실무에서는 `REQUIRED`가 거의 전부. 간혹 `REQUIRES_NEW` 사용. 나머지는 참고 수준으로만 알아두면 된다.
`isolation`, `timeout`, `readOnly`는 트랜잭션이 **처음 시작될 때만** 적용되며, 참여(join) 시에는 무시된다.
:::

## 스프링 트랜잭션 전파 - 활용

### 예제 구조

```
MemberService (joinV1/joinV2)
    ├── MemberRepository.save() @Transactional
    └── LogRepository.save()    @Transactional
                                → "로그예외" 포함 시 RuntimeException 발생
```

### 서비스 트랜잭션 OFF - 각각 독립 트랜잭션

**성공 케이스** : MemberRepository(트랜잭션B), LogRepository(트랜잭션C) 각각 커밋. 둘 다 저장됨.

**실패 케이스** : LogRepository에서 예외 발생 시 트랜잭션C만 롤백. **트랜잭션B는 이미 커밋된 상태.**

```
결과: Member 저장됨 (B 커밋) / Log 롤백됨 (C 롤백)
→ 데이터 정합성 문제 발생
```

### 서비스 트랜잭션 ON - 단일 트랜잭션

가장 단순한 해결책. MemberService에만 `@Transactional`, 나머지는 제거.

```java
@Transactional  // MemberService에만
public void joinV1(String username)
```

MemberRepository, LogRepository는 트랜잭션 AOP 없이 MemberService의 커넥션을 그냥 사용. 하나의 물리 트랜잭션으로 묶임. `@Transactional`은 해당 애노테이션이 적용된 클래스/메서드에만 AOP 프록시가 생성된다.

:::tip
단, MemberRepository를 단독으로 호출하는 클라이언트B가 있다면 이 방식은 불가. 그래서 트랜잭션 전파가 필요하다.
:::

내부 데이터 접근 과정에서 예외 발생시 `RuntimeException`을 던지기만 하고 롤백을 요청하는 주체가 없다.

### 전파 커밋 (Service + Repository 모두 @Transactional ON)

```
MemberService     → 논리 트랜잭션A (신규, 외부)
MemberRepository  → 논리 트랜잭션B (참여, 내부)
LogRepository     → 논리 트랜잭션C (참여, 내부)
```

-   B, C는 `isNewTransaction=false` → 커밋 요청 무시
-   A만 실제 물리 커밋 수행
-   모두 성공 시 전체 커밋

### 전파 롤백

LogRepository에서 예외 발생 시:

```
LogRepository 예외 → 트랜잭션C AOP → rollbackOnly=true 설정
                   → 예외를 MemberService로 전달
MemberService      → 예외를 잡지 않음 → 트랜잭션A AOP → 물리 롤백
```

결과: Member, Log 모두 롤백 → 데이터 정합성 유지

### 복구 시도 실패 - REQUIRED (recoverException_fail)

요구사항: **로그 저장 실패해도 회원 가입은 유지**

단순하게 MemberService에서 예외를 catch해서 정상 흐름으로 변환 시도.

```java
// joinV2
try {
    logRepository.save(logMessage);
} catch (RuntimeException e) {
    log.info("정상 흐름 변환"); // 예외 복구 시도
}
```

:::danger 실패 이유
LogRepository 롤백 시 이미 `rollbackOnly=true` 가 ThreadLocal에 세팅된 상태.
MemberService에서 예외를 잡아도 플래그는 그대로.
MemberService의 트랜잭션 AOP가 커밋 시도 → rollbackOnly 확인 → 물리 롤백 → `UnexpectedRollbackException` 발생.

**catch로 예외를 삼켜도 rollbackOnly 플래그는 초기화되지 않는다.**
:::

### 복구 성공 - REQUIRES_NEW (recoverException_success)

LogRepository를 **별도 물리 트랜잭션**으로 분리.

```java
@Transactional(propagation = Propagation.REQUIRES_NEW)
public void save(Log logMessage)
```

```
물리 트랜잭션1: MemberService(A) + MemberRepository(B) → con1
물리 트랜잭션2: LogRepository(C) REQUIRES_NEW          → con2 (별도)
```

흐름:

```
LogRepository 예외 → 신규 트랜잭션이므로 물리 롤백(con2) → rollbackOnly 설정 없음
                  → 예외를 MemberService로 전달
MemberService     → 예외 catch → 정상 흐름 반환
                  → 트랜잭션A AOP 커밋 → rollbackOnly 없음 → 물리 커밋(con1)
```

결과: **Member 저장됨 / Log만 롤백됨**

:::warning REQUIRES_NEW 주의사항
하나의 HTTP 요청에 DB 커넥션을 동시에 2개 사용. 커넥션 풀 고갈 위험.
성능이 중요한 서비스라면 아래 구조 대안을 고려.
:::

### REQUIRES_NEW 대안 - Facade 패턴

```
클라이언트A → MemberFacade
                ├── memberService.join()     → 물리 트랜잭션1 (순차)
                └── logRepository.save()     → 물리 트랜잭션2 (순차)
```

커넥션을 동시에 2개 쓰지 않고 순차적으로 사용. 구조가 허용된다면 이 방식이 더 안전.
